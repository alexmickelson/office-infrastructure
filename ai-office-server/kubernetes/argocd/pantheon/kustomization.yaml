apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://raw.githubusercontent.com/alexmickelson/ai_pantheon/main/kube/postgres.yaml
  - https://raw.githubusercontent.com/alexmickelson/ai_pantheon/main/kube/app.yaml

configMapGenerator:
  - name: app-config
    namespace: pantheon
    behavior: merge
    literals:
      - PHX_HOST=alexpantheon.snowse.io
      - OIDC_REDIRECT_URI=https://alexpantheon.snowse.io/auth/callback
      - POOL_SIZE=10
      - PORT=4000

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: pantheon
        namespace: pantheon
      spec:
        replicas: 1
        selector:
          matchLabels:
            app: pantheon
        template:
          metadata:
            labels:
              app: pantheon
          spec:
            containers:
              - name: pantheon
                image: ghcr.io/alexmickelson/pantheon-gateway:31@sha256:8c637659c9435c811676bdef340b4d1a78e8a2bfb424f25dcd1c0ce920894410
  - patch: |-
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: pantheon
        namespace: pantheon
      spec:
        parentRefs:
          - group: gateway.networking.k8s.io	
            kind: Gateway	
            name: main-gateway
            namespace: istio-system
        hostnames:
          - "alexpantheon.snowse.io"
